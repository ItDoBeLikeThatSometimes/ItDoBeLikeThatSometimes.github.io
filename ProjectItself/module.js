// Using require to import the necessary functions from the bubblesets library
import { BSplineShapeGenerator, BubbleSet, PointPath, ShapeSimplifier} from "/bubblesets/bubblesets.js";

// Create a new instance of BubbleSet
const uba = new BubbleSet();

// Log a message to confirm the script is running
console.log('joe mama');

// Log the created BubbleSet instance
console.log(uba);
